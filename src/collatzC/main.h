#include <iostream>
#include <stdlib.h>
#include <vector>

#ifndef  MAIN_H
#define MAIN_H

//typedef std::vector<int64_t> int64_vector_t;
//const int CACHE_PREALLOC = 200;
//int64_t* termCount(int64_t max);

int64_t colCalc(int64_t number, int64_t count);
int64_t countTerms(int64_t max);

#endif
